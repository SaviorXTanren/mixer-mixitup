<Controls:MainControlBase x:Class="MixItUp.WPF.Controls.Chat.ChatCommandsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MixItUp.WPF.Controls.Chat"
             xmlns:Controls="clr-namespace:MixItUp.WPF.Controls"
             xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
                          
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"   

             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" x:Name="PreMadeCommandsButton" Click="PreMadeCommandsButton_Click" IsEnabled="False">Pre-Made Commands</Button>
            <Button Grid.Column="1" x:Name="CustomCommandsButton" Click="CustomCommandsButton_Click" IsEnabled="False">Custom Commands</Button>
            
        </Grid>

        <Grid x:Name="PreMadeCommandsGrid" Grid.Row="2" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="65" />
                    <ColumnDefinition Width="60" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" FontWeight="Bold" HorizontalAlignment="Center" Margin="-30,0,0,0">Name</TextBlock>
                <TextBlock Grid.Column="1" FontWeight="Bold" HorizontalAlignment="Center" Margin="-30,0,0,0">Commands</TextBlock>
                <TextBlock Grid.Column="2" FontWeight="Bold" HorizontalAlignment="Center" Margin="-40,0,0,0">Permission</TextBlock>
                <TextBlock Grid.Column="3" FontWeight="Bold" HorizontalAlignment="Center" Margin="-30,0,0,0">Cooldown</TextBlock>
            </Grid>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Visible">
                <StackPanel>
                    <local:PreMadeChatCommandControl x:Name="FollowAgeChatCommand" Margin="0,5" />
                    <local:PreMadeChatCommandControl x:Name="GameChatCommand" Margin="0,5" />
                    <local:PreMadeChatCommandControl x:Name="GiveawayChatCommand" Margin="0,5" />
                    <local:PreMadeChatCommandControl x:Name="MixerAgeChatCommand" Margin="0,5" />
                    <local:PreMadeChatCommandControl x:Name="PurgeChatCommand" Margin="0,5" />
                    <local:PreMadeChatCommandControl x:Name="QuoteChatCommand" Margin="0,5" />
                    <local:PreMadeChatCommandControl x:Name="SparksChatCommand" Margin="0,5" />
                    <local:PreMadeChatCommandControl x:Name="StreamerAgeChatCommand" Margin="0,5" />
                    <local:PreMadeChatCommandControl x:Name="TimeoutChatCommand" Margin="0,5" />
                    <local:PreMadeChatCommandControl x:Name="TitleChatCommand" Margin="0,5" />
                    <local:PreMadeChatCommandControl x:Name="UptimeChatCommand" Margin="0,5" />
                </StackPanel>
            </ScrollViewer>

        </Grid>

        <Grid x:Name="CustomCommandsGrid" Grid.Row="2" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="20" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
            </Grid.RowDefinitions>

            <DataGrid x:Name="CustomCommandsListView" Grid.Row="0" Style="{StaticResource StandardDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True" Width="2*" />
                    <DataGridTextColumn Header="Commands" Binding="{Binding CommandsString}" IsReadOnly="True" Width="*" />
                    <DataGridTextColumn Header="Permission" Binding="{Binding PermissionsString}" IsReadOnly="True" Width="*" />
                    <DataGridTemplateColumn Width="65">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button x:Name="TestButton" Click="TestButton_Click" Width="40" HorizontalAlignment="Center">
                                    <MaterialDesign:PackIcon Kind="Play" Height="24" Width="24" Margin="-13,-10" Foreground="{StaticResource WhiteColor}" />
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Width="65">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button x:Name="EditButton" Click="EditButton_Click" Width="40" HorizontalAlignment="Center">
                                    <MaterialDesign:PackIcon Kind="TooltipEdit" Height="24" Width="24" Margin="-13,-10" Foreground="{StaticResource WhiteColor}" />
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Width="65">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button x:Name="DeleteButton" Click="DeleteButton_Click" Width="40" HorizontalAlignment="Center">
                                    <MaterialDesign:PackIcon Kind="Delete" Height="24" Width="24" Margin="-13,-10" Foreground="{StaticResource WhiteColor}" />
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ToggleButton x:Name="EnableDisableToggleSwitch" Style="{StaticResource MaterialDesignSwitchToggleButton}" VerticalAlignment="Center" HorizontalAlignment="Center" ToolTip="Enable/Disable Command" IsChecked="{Binding IsEnabled}" Checked="EnableDisableToggleSwitch_Checked" Unchecked="EnableDisableToggleSwitch_Unchecked" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <Button x:Name="AddCommandButton" Grid.Row="2" Click="AddCommandButton_Click" Width="130">New Command</Button>
        </Grid>
    </Grid>
</Controls:MainControlBase>
