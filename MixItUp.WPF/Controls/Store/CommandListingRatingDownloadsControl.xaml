<UserControl x:Class="MixItUp.WPF.Controls.Store.CommandListingRatingDownloadsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MixItUp.WPF.Controls.Store"
             xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="100">
    
    <Grid Grid.Row="2" HorizontalAlignment="Right" Margin="0,0,2,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="{Binding AverageRatingDisplayString}" VerticalAlignment="Center" />
        <MaterialDesign:PackIcon Grid.Column="1" Kind="Star" VerticalAlignment="Center" Margin="3,0,0,1" />

        <TextBlock Grid.Column="3" Text="{Binding TotalDownloads}" VerticalAlignment="Center" />
        <MaterialDesign:PackIcon Grid.Column="4" Kind="Download" VerticalAlignment="Center" Margin="3,0,0,0" />
    </Grid>
</UserControl>
