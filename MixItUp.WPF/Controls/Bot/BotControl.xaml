<Controls:MainControlBase x:Class="MixItUp.WPF.Controls.Bot.BotControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MixItUp.WPF.Controls.Bot"
             xmlns:Controls="clr-namespace:MixItUp.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        
        <Grid x:Name="ExistingBotGrid" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="20" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0">Logged in:</TextBlock>
                <TextBlock Grid.Column="2" x:Name="BotLoggedInNameTextBlock" />
            </Grid>

            <Button x:Name="LogOutBotButton" Grid.Row="2" Width="80" Click="LogOutBotButton_Click">Log Out Bot</Button>
        </Grid>

        <Grid x:Name="NewBotLoginGrid" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="20" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Button x:Name="LogInBotButton" Grid.Row="0" Click="LogInBotButton_Click" Width="80">Log In Bot</Button>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0">Short Code:</TextBlock>
                <TextBox Grid.Column="2" x:Name="BotShortCodeTextBox" IsReadOnly="True" Width="60" />
            </Grid>
        </Grid>

    </Grid>
</Controls:MainControlBase>
