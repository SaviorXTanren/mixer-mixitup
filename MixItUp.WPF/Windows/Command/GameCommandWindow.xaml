<Windows:LoadingWindowBase x:Class="MixItUp.WPF.Windows.Command.GameCommandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MixItUp.WPF.Windows.Command"
        xmlns:Controls="clr-namespace:MixItUp.WPF.Controls"
        xmlns:CurrencyControls="clr-namespace:MixItUp.WPF.Controls.Currency"
        xmlns:GameControls="clr-namespace:MixItUp.WPF.Controls.Games"
        xmlns:Windows="clr-namespace:MixItUp.WPF.Windows"
        xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
                                        
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"   
                  
        Icon="./../../Logo.ico"
        Title="Customize Game" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <MaterialDesign:DialogHost x:Name="MDDialogHost" Identifier="RootDialog" />

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Visible">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" x:Name="GameNameTextBox" MaterialDesign:HintAssist.Hint="Game Name" Width="150" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />

                    <TextBox x:Name="GameChatCommandTextBox" Grid.Column="2" MaterialDesign:HintAssist.Hint="Commands (letters &amp; numbers only, space seperated)" Width="350" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />

                </Grid>

                <Grid Grid.Row="2" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="GameCooldownTextBox" Grid.Column="0" MaterialDesign:HintAssist.Hint="Cooldown (Seconds)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />

                    <ComboBox x:Name="GameLowestRoleAllowedComboBox" Grid.Column="2" MaterialDesign:HintAssist.Hint="Permissions" Style="{StaticResource MaterialDesignFloatingHintComboBox}" />

                    <CurrencyControls:RankSelectorControl x:Name="RankSelector" Grid.Column="4" />

                </Grid>

                <Grid Grid.Row="4" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <CurrencyControls:CurrencySelectorControl x:Name="CurrencySelector" Grid.Column="0" />

                    <TextBlock Grid.Column="2" Style="{StaticResource HeaderTextBlock}" VerticalAlignment="Center">Multiplayer:</TextBlock>
                    <ToggleButton x:Name="IsMultiplayerToggleSwitch" Grid.Column="4" Style="{StaticResource MaterialDesignSwitchToggleButton}" VerticalAlignment="Center" HorizontalAlignment="Center" ToolTip="Multiplayer" Checked="IsMultiplayerToggleSwitch_Checked" Unchecked="IsMultiplayerToggleSwitch_Checked" />

                </Grid>

                <Grid Grid.Row="6" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0">
                        <Hyperlink NavigateUri="https://github.com/SaviorXTanren/mixer-mixitup/wiki/Special-Identifiers" RequestNavigate="Hyperlink_RequestNavigate">Special Identifiers Reference</Hyperlink>
                    </TextBlock>

                    <TextBlock Grid.Column="2">
                        <Hyperlink NavigateUri="https://github.com/SaviorXTanren/mixer-mixitup/wiki/Games" RequestNavigate="Hyperlink_RequestNavigate">Games Reference</Hyperlink>
                    </TextBlock>

                </Grid>

                <Grid x:Name="MultiplayerGrid" Grid.Row="8" HorizontalAlignment="Center" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="20" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="20" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" HorizontalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="GameLengthTextBox" Grid.Column="0" MaterialDesign:HintAssist.Hint="Game Length (Seconds)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />

                        <TextBox x:Name="GameMinimumParticipantsTextBox" Grid.Column="2" MaterialDesign:HintAssist.Hint="Min Participants" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />

                        <ComboBox x:Name="GameResultTypeComboBox" Grid.Column="4" Width="200" MaterialDesign:HintAssist.Hint="Result Type" Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
                    </Grid>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <GroupBox Header="Game Started Command" Grid.Column="0" Width="250">
                            <GameControls:GameStepCommandControl x:Name="GameStartedCommandControl" />
                        </GroupBox>

                        <GroupBox Header="Game Ended Command" Grid.Column="2" Width="250">
                            <GameControls:GameStepCommandControl x:Name="GameEndedCommandControl" />
                        </GroupBox>

                    </Grid>

                    <Grid Grid.Row="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <GroupBox Header="User Joined Command" Grid.Column="0" Width="250">
                            <GameControls:GameStepCommandControl x:Name="UserJoinedCommandControl" />

                        </GroupBox>

                        <GroupBox Header="Not Enough Users Command" Grid.Column="2" Width="250">
                            <GameControls:GameStepCommandControl x:Name="NotEnoughUsersCommandControl" />
                        </GroupBox>

                    </Grid>
                </Grid>

                <Grid Grid.Row="10" HorizontalAlignment="Center" Height="230">
                    <GroupBox Width="700">
                        <GroupBox.Header>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="10,0,0,0" Style="{StaticResource HeaderTextBlock}" Foreground="{StaticResource WhiteColor}">Possible Results</TextBlock>

                                <Button x:Name="AddProbabilityButton" Grid.Column="2" Width="110" Click="AddProbabilityButton_Click">Add Result</Button>
                            </Grid>
                        </GroupBox.Header>

                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ListView x:Name="ResultsProbabilityListView" HorizontalContentAlignment="Stretch" />
                        </ScrollViewer>
                    </GroupBox>
                </Grid>

                <Grid Grid.Row="12">
                    <Button x:Name="SaveButton" Width="80" Click="SaveButton_Click">Save</Button>
                </Grid>

            </Grid>
        </ScrollViewer>

        <Controls:LoadingStatusBar x:Name="StatusBar" Grid.Row="1" />
    </Grid>
</Windows:LoadingWindowBase>
